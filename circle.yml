machine:
  node:
    version: 5.1.0

test:
  override:
    - npm run build && npm run test

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: chloeandisabel
    commands:
      - ./resources/npm-login.sh
      - npm publish
